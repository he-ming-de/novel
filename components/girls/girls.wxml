<!--components/girls/girls.wxml-->
<view wx:for="{{females}}" wx:key="index" bindtap="front" data-item="{{item}}">
	<view class="item">
		<image src="{{item.cover}}" class="img"></image>
    {{item.title}}
	</view>
</view>
